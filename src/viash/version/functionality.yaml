name: version
description: Version all functionality and platform YAML files recursively
arguments:
  - name: "version"
    type: string
    required: true
  - name: "--location"
    type: file
    alternatives: ["-l"]
    default: "src"
resources:
  - type: bash_script
    path: ./code.sh
version: 0.2.0-rc2
