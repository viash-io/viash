functionality:
  name: operator_conflict
  namespace: test_wfs
  resources:
    - type: nextflow_script
      path: main.nf
      entrypoint: base
    # TODO: make absolute when the ns build uses the right CWD
    - path: ../../../resources
  dependencies:
    - name: concat
platforms:
  - type: nextflow